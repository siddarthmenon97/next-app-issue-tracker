'use client';
import React from "react";

const AddToCart = () => {
    return (
        <button onClick={() => console.log('clicked')}>
            Add button
        </button>
    )
}

export default AddToCart;